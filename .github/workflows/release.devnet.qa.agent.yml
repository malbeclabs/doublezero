name: release.devnet.qa.agent.daily

on:
  workflow_dispatch:

jobs:
  release:
    uses: ./.github/workflows/release.daily.yml
    with:
      component: qa-agent
      playbook: playbooks/qa_agents.yml
    secrets: inherit
    permissions:
      contents: write
      packages: write
