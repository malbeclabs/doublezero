# yaml-language-server: $schema=https://goreleaser.com/static/schema-pro.json
# vim: set ts=2 sw=2 tw=0 fo=cnqoj

version: 2

project_name: doublezero-solana-validator-debt

monorepo:
  tag_prefix: solana-validator-debt/

builds:
  - id: doublezero-solana-validator-debt
    binary: doublezero-solana-validator-debt
    builder: rust
    tool: cargo
    command: build
    flags:
      - --package=doublezero-solana-validator-debt
      - --release
    targets:
      - x86_64-unknown-linux-gnu
    env:
      - RUSTFLAGS=-C linker=x86_64-linux-gnu-gcc
      - BUILD_VERSION={{ .Version }}
      - BUILD_COMMIT={{ .ShortCommit }}
      - DATE={{ .Date }}

archives:
  - id: doublezero_solana_validator_debt_archive
    formats: ["tar.gz"]
    ids: [doublezero-solana-validator-debt]
    # this name template makes the OS and Arch compatible with the results of `uname`
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}

nfpms:
  - id: doublezero-solana-validator-debt
    package_name: doublezero-solana-validator-debt
    ids: [doublezero-solana-validator-debt]
    vendor: doublezero
    homepage: doublezero.xyz
    maintainer: tech <tech@malbeclabs.com>
    description: |-
      DoubleZero Solana Validator Debt
    license: Apache 2.0
    formats:
      - deb
    bindir: /usr/local/bin
    release: 1
    section: default
    contents:
      - src: release/packaging/systemd/doublezero-solana-validator-debt.service
        dst: /lib/systemd/system/doublezero-solana-validator-debt.service
        type: config
    overrides:
      rpm:
        contents:
          - src: release/packaging/systemd/doublezero-solana-validator-debt.service
            dst: /usr/lib/systemd/system/doublezero-solana-validator-debt.service
            type: config

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  github:
    owner: doublezerofoundation
    name: doublezero-offchain
  draft: false
  replace_existing_artifacts: true

announce:
  slack:
    enabled: true
    message_template: "DoubleZero Solana Validator Debt {{ .Tag }} has been released! Check it out at {{ .ReleaseURL }}"
    channel: "#bots"

git:
  ignore_tags:
    - solana-validator-debt/daily

nightly:
  publish_release: true
  keep_single_release: true
  version_template: "{{ incpatch .Version }}~git{{ .Env.BUILD_DATE }}.{{ .ShortCommit }}"
  tag_name: "solana-validator-debt/daily"

cloudsmiths:
  - organization: malbeclabs
    repository: doublezero
    distributions:
      deb: "any-distro/any-version"
      rpm: "any-distro/any-version"
