name: release.devnet.agent.daily

on:
  workflow_dispatch:

jobs:
  release:
    uses: ./.github/workflows/release.daily.yml
    with:
      component: agent
    secrets: inherit
    permissions:
      contents: write
      packages: write
