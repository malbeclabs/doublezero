name: release.devnet.activator.daily

on:
  workflow_dispatch:
    inputs:
      infra_branch:
        description: 'Branch of malbeclabs/infra to use for ansible playbooks'
        required: false
        type: string
        default: ''

jobs:
  release:
    uses: ./.github/workflows/release.daily.yml
    with:
      component: activator
      playbook: playbooks/activators.yml
      infra_branch: ${{ inputs.infra_branch }}
    secrets: inherit
    permissions:
      contents: write
      packages: write
