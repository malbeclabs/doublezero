name: release.devnet.geoprobe-agent.daily

on:
  workflow_dispatch:

jobs:
  release:
    uses: ./.github/workflows/release.daily.yml
    with:
      component: geoprobe-agent
      playbook: playbooks/geoprobe_agents.yml
    secrets: inherit
    permissions:
      contents: write
      packages: write