name: release.devnet.client.daily

on:
  workflow_dispatch:

jobs:
  release:
    uses: ./.github/workflows/release.daily.yml
    with:
      component: client
      playbook: playbooks/validators.yml
    secrets: inherit
    permissions:
      contents: write
      packages: write
