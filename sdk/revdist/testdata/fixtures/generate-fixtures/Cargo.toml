[package]
name = "generate-fixtures"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
bytemuck = { version = "1", features = ["derive"] }
doublezero-program-tools = { path = "../../../../../solana/crates/program-tools" }
doublezero-revenue-distribution = { path = "../../../../../solana/programs/revenue-distribution" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ruint = "1"
solana-pubkey = { version = "3", features = ["bytemuck"] }
